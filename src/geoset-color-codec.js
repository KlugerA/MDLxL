/** Keep authored MDX geoset colors in their original channel order.
 * Display code may interpret those values, but loading and saving must not
 * rewrite the model's RGB data. */
export function convertMdxGeosetColorTracks(animations = []) {
  return animations;
}
